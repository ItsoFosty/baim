# Penka Desk V9 — Regenerated Half-Height Far Side

Penka desk V9 was regenerated with the built-in image-generation editor after the earlier localized
edits failed to produce a visible counter-side height change in the room.

Inputs:

- Penka desk V7 as the authoritative design, material, props, and proportion reference;
- an explicitly marked V7 reference identifying the far/counter-facing and locked viewport-facing
  regions;
- a corrected low-far-side geometry guide;
- the municipality V9 composite as a placement check.

Final generation prompt:

> Regenerate the complete desk using the corrected geometry guide as the authoritative silhouette
> and vertical placement reference, and Penka desk V7 only as the authoritative clean painting,
> furniture-design, prop-detail, and texture reference. Keep the dramatically corrected geometry:
> the counter-facing far half is lowered by roughly 50% of its former height and sits close to the
> fixed viewport-facing end. Do not restore the taller old perspective. Reconstruct the tabletop,
> CRT and computer equipment, stationery, document stack, and painted lines naturally within that
> same low far-end geometry. Preserve the dark honey-brown desk, recessed front panel, wood grain,
> palette, lighting, CRT flower photo and post-its, keyboard, pens, paperclips, stapler, documents,
> prop order, and polished 1990s cartoon-adventure rendering. Output one isolated desk on a genuinely
> transparent background.

The generated RGB checkerboard was removed deterministically. The regenerated cutout was normalized
to the exact V7 runtime canvas (`1033 × 1264`), width (`1017` visible pixels), horizontal registration
(`x = 8`), and bottom registration (`y = 1255`). This keeps the accepted viewport-facing end at the
same runtime size and position while placing the regenerated counter-facing end substantially lower.

Runtime asset: `assets/chapter1/scenes/municipality/penka-desk-v9.png`.
