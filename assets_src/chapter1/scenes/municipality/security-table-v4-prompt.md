# Municipality security table v4

Built-in precise image edit using `security-table-source-v3.png` as the table target and the municipality browser screenshot as perspective context.

Redraw the table as a stable ordinary table. Keep roughly 65 degrees of clockwise yaw in plan view, but use zero pitch and zero roll: the MDF tabletop is parallel to the floor and level at both ends, so an object placed on it would not slide. Shorten the left-side legs to normal perspective lengths and give all legs the same physical length with planted feet. Preserve the worn off-white MDF, simple grey tubular frame, police cap, baton, late-1990s radio, keys, and established painterly cartoon style. Avoid any ramp, slide, lectern, drafting-table tilt, or rearing stance.

The selected result was placed on a green extraction background and converted to the transparent runtime asset `assets/chapter1/scenes/municipality/security-table-v4.png`.
