# Municipality security table v6

Built-in precise image edits using the v5 table and the user's `Screenshot From 2026-08-28 18-42-02.png` as the authoritative geometry reference.

Replace the steep tabletop with the shallow, low parallelogram indicated by the user's blue outline. The tabletop must be much wider than it is tall in screen space, with its back edge only slightly above the front edge. Use four straight pipe legs below its corners, preserve the worn off-white MDF, cap, baton, radio, keys, and painterly style, and keep the complete prop approximately square so it can be fitted to the outlined region without a wide landscape footprint.

The selected result was placed on a green extraction background and converted to the transparent runtime asset `assets/chapter1/scenes/municipality/security-table-v6.png`. Its authored scene rectangle intentionally uses both width and height to match the user's blue in-scene outline.
